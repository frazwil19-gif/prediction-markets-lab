"""Command-line entry point for Prediction Markets Lab.

PLACEHOLDER — deferred beyond Stage 1.

Stage 1 exposes only importable Python modules and their unit tests;
there is no user-facing CLI yet. A simple CLI (e.g. `pml calc-ev`,
`pml grade`) is planned for Stage 2 alongside the manual CSV/Sheets
workflow. See docs/ROADMAP.md.
"""

from __future__ import annotations


def main() -> None:
    """Entry point placeholder. Not yet implemented."""
    raise NotImplementedError(
        "The CLI is planned for Stage 2. In Stage 1, import "
        "prediction_markets_lab modules directly."
    )


if __name__ == "__main__":
    main()
